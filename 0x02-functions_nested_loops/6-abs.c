#include "main.h"
/**
 * main - Entry point of the program
 * Description: This function prints the absolute value
 * Return: n (Value)
 */
int _abs(int n)
	{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
	}
